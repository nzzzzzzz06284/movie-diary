# 开启 Hermes 网关（让「观影手记」接入小助手）

观影手记右下角的 💬 助手按钮已经做好了，它会调用你**电脑上正在运行的 Hermes 本地网关**。
但 Hermes 默认**没有开启这个网关**，所以需要在 Hermes 那一侧开一下，网页才能连上。

---

## 一、我已从 Hermes 源码确认的接口事实（不是猜的）

| 项目 | 值 |
|---|---|
| 网关地址 | `http://localhost:8642`（默认端口 8642，**只监听本机 127.0.0.1**） |
| 对话接口 | `POST /v1/chat/completions`（和 OpenAI 格式一样） |
| 跨域 CORS | 默认**不放行**，必须设为 `*` 否则网页会被浏览器拦（HTTP 403） |
| 鉴权 | 可选。Hermes 网关若设了 API Key，网页里也要填同样的 Key |
| 开启状态 | api_server 是个**默认关闭**的平台，需手动启用 |

> 结论：助手只能在你**自己电脑**上打开 app 时使用（手机端开 app 调不到你电脑的 Hermes，这是正常的）。

---

## 〇、✅ 已经帮你改好的部分（2026-08-24，经你授权）

在你授权下，我已帮你改好了 Hermes 的配置文件 `C:\Users\PZ\AppData\Local\hermes\config.yaml`：

- ✅ 启用了 `platforms.api_server` 平台（默认端口 8642，监听本机 127.0.0.1）
- ✅ CORS 已设为 `*`（放行网页跨域）
- ✅ 设了 API Key：`cbf2807900e41ee7834a4365b86b56cb`
- ✅ 原配置已备份为 `config.yaml.bak.20260824`（改坏了随时能还原）
- ✅ 已用 YAML 解析器验证配置合法

所以下面「二、你需要做的」里，**启用平台和 CORS 不用再管了**，你只需要：

1. **重启 Hermes**（让新配置生效）
2. 打开观影手记 → 设置 → 🤖 Hermes 智能助手 → **API Key 填**：`cbf2807900e41ee7834a4365b86b56cb`
3. 点「测试连接」，显示 **连接成功 ✅** 就通了
4. 点右下角 💬 气泡，开聊

> 注意：这个 Key 相当于你家 Hermes 的钥匙，**不要发到网上/公开的地方**。

---

## 二、你需要做的（开启 Hermes 网关）

### 方式 A：在 Hermes 桌面端开启（最省事，先试这个）
1. 打开 **Hermes 桌面端** → 设置 → 找「平台 / Platforms / API Server（开放接口）」类开关，**开启**它。
2. 在同一处（或 Hermes 的环境变量/配置）加一项：
   `API_SERVER_CORS_ORIGINS = *`（星号，放行所有网页源）
   - 想更严格：只填你的网页源 `https://nzzzzzzz06284.github.io`
3. 重启 Hermes，让配置生效。

### 方式 B：改 Hermes 配置文件（若桌面端找不到开关）
在 Hermes 的配置文件里加入（报错信息已证实路径是 `config.yaml` 的 `platforms.api_server` 段）：

```yaml
platforms:
  api_server:
    enabled: true
    host: 127.0.0.1
    port: 8642
    cors_origins: "*"
    key: ""          # 留空=不鉴权；想设就填一个你自己记的字符串，网页里也要填同样的值
```

保存后**重启 Hermes**。

---

## 三、在观影手记里接通

1. 打开观影手记 → 底部「设置」→ **🤖 Hermes 智能助手**
2. 网关地址填 `http://localhost:8642`（若上面设了 key，也填上）
3. 点 **「测试连接」**：
   - 显示 `连接成功 ✅` → 通了
   - `403` → CORS 没设 `*`；`401` → Key 不对；连接失败 → Hermes 没开 / 网关没启用 / 地址端口错
4. 返回任意页面，点右下角 💬 气泡，就能聊了。

---

## 四、助手能做什么（已接好）

- 读你电影库的**真实数据**当上下文，所以推荐/总结都基于你真的看过的片
- 快捷指令：**📊 年度报告**（温暖具体的年度观影总结）、**🎬 推荐电影**（按你偏好推荐）、**📝 观影口味**（总结你的口味）
- 多轮对话（用 `X-Hermes-Session-Id` 维持，换话题点 ↺ 开新对话）

---

## 五、嫌麻烦？

如果你找不到开启入口，或不想自己改配置文件，**告诉我一声，我可以：**
1. 帮你查 Hermes 确切的「启用 API Server」入口（再读它的代码确认）；
2. 在你**授权**下，直接帮你改 Hermes 的配置文件把网关开好、CORS 设好。

（改的是你另一个应用 Hermes 的文件，所以必须你点头我才动。）

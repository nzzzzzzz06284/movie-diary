# 观影手记 · 部署到手机（纯网页上传，不用下载任何软件）

这份教程假设你从没用过 GitHub、也不会写代码，而且**不想安装任何电脑软件**。全部操作都在浏览器网页 [github.com](https://github.com) 上完成，照着点就行，大约 10 分钟。

下面的按钮名称都按**中文界面**写的，你照着对屏幕上的字即可（个别地方 GitHub 中文版仍显示英文，我会括号标注，比如「页面（Pages）」）。

> 你选了 **方案 A（公开仓库，免费）**。先记住一句话：**公开的是"代码"，不是你的电影日记**。你记的每一条内容都存在你自己手机的浏览器里，不经过 GitHub，别人看不到。仓库公开只代表"这个 app 是怎么写的"能被看到，不含你的任何数据。

---

## 准备：确认电脑上的 app 文件夹

先把电脑文件管理器打开，确认这个文件夹存在：

```
D:/工作空间/movie-diary
```

点进去，里面应该有这些（不用管具体是什么，确认它们在就行）：
- `index.html`
- 一个 `css` 文件夹、一个 `js` 文件夹、一个 `assets` 文件夹
- `manifest.webmanifest`
- 一个名字以 `.` 开头的隐藏文件 `.nojekyll`（在文件管理器里可能要开"显示隐藏文件"才看得到）

---

## 第 1 步：在网页上新建一个仓库

1. 用浏览器登录 [github.com](https://github.com)（中文界面的话，顶栏有「首页 / 仓库 / 拉取请求」等）。
2. 右上角有个 **`+`**（加号）按钮 → 点它 → 选 **新建仓库**。
3. 在新建页面填：
   - **仓库名称（Repository name）**：填 `movie-diary`（用小写字母和横线，别用中文和空格）
   - **可见性**：选 **公开**（我们要免费开 Pages，必须公开）
   - **下方几个勾选框**（如「添加 README 文件」「.gitignore」「许可证」）：**都不要勾**
   - 点 **创建仓库**。
4. 创建后会进入一个**空仓库页面**（中间会提示"这个仓库是空的"或给出一些命令行说明）。

---

## 第 2 步：把 app 文件夹里的东西上传上去

> ⚠️ **关键：拖"文件夹里面的内容"，不要拖 `movie-diary` 这个文件夹本身。** 否则网址会多套一层、打不开。

1. 在空仓库页面，点 **添加文件（Add file）** 下拉 → 选 **上传文件（Upload files）**。
2. 现在会进入上传界面，中间有个大框「把文件拖到这里，或选用你的文件」。
3. 回到电脑文件管理器，打开 `D:/工作空间/movie-diary` 文件夹。
4. 在文件夹里**按 `Ctrl + A` 全选所有内容**（就是第 1 步列出的那些），把它们**一起拖进** GitHub 网页的那个大框里。
   - 拖进去后，你应该看到 `index.html`、`css/`、`js/`、`assets/` 这些**直接出现在上传列表里**（css、js、assets 应该显示为带文件夹图标的条目，里面还有子文件）。
   - ✅ 正确样子：列表顶层是 `index.html`、`css`、`js`、`assets`、`manifest.webmanifest`、`.nojekyll`
   - ❌ 错误样子：列表顶层只有一个 `movie-diary` 文件夹（说明你拖错了，把外层文件夹也拖进去了）。**遇到这种情况，把上传框里的东西删掉重来。**
5. 拖完后，网页下方有个 **提交更改（Commit changes）** 按钮（有时要往下滚一点）。点它。
   - 提交说明（Commit message）可以随便写，比如「初版观影手记」，不写也行。
6. 等几秒，页面跳回仓库，你应该能看到 `index.html`、`css`、`js` 这些文件和文件夹了——说明上传成功。

> 关于那个隐藏文件 `.nojekyll`：拖文件夹时它不一定会被带上去（因为是隐藏文件）。**没关系，本项目不需要它**（我们的文件名里没有下划线开头的，不会出问题）。如果你想要它，上传完成后在仓库里点 **添加文件 → 创建新文件**，文件名填 `.nojekyll`、内容留空，提交即可——不补也完全不影响使用。

---

## 第 3 步：打开"能用浏览器访问"的开关（GitHub Pages）

代码传好了，但还不能直接用浏览器打开，要打开这个开关：

1. 在仓库页面，点顶部导航的 **设置（Settings）**。
2. 左侧菜单找 **页面（Pages）**（在"代码和自动化"分类下；中文版有时仍显示英文 "Pages"）。
3. 在「构建和部署 → 源（Source）」这里：
   - 选 **从分支部署（Deploy from a branch）**
   - **分支（Branch）**：选 `main`，右边目录选 **/(根目录)**（中文有时显示「根目录」或仍是 `/(root)`）
   - 点 **保存（Save）**。
4. 页面上方会出现一行提示，告诉你的网址大概长这样：
   ```
   https://你的用户名.github.io/movie-diary/
   ```
   **等 1–2 分钟**让 GitHub 生成（第一次要时间）。

---

## 第 4 步：在手机上打开，并"加到主屏幕"

1. 手机浏览器（iPhone 用 Safari，安卓用 Chrome）打开上面的网址（把"你的用户名"换成你 GitHub 的用户名，全小写）。
   - 例：用户名是 `liaoliao`，网址就是 `https://liaoliao.github.io/movie-diary/`
2. 打开后，点浏览器的 **分享按钮**（iOS 在底部工具栏，安卓在右上角 ⋮ 菜单）→ 选 **「添加到主屏幕」**。
3. 桌面上会出现一个「观影手记」图标，点开就像 App 一样用了，**还能离线打开**。

---

## 重要：你的数据怎么备份（一定要看）

这个 app 没有服务器，数据**只存在你当前这部手机的浏览器里**。这意味着：

- 换手机、清浏览器缓存、卸载重装，记录会没——**所以要定期备份**。
- 备份方法：app 里点底部 **⚙️ 设置** → **导出备份**，会下载一个 `.json` 文件，存到网盘或发给自己。
- 换设备后：先打开 app，再到 **设置 → 导入备份** 把这个 `.json` 传回去即可。

---

## 常见问题

**Q：打开网址是 404 / 页面空白？**
A：① 等 1–2 分钟再刷新（GitHub 第一次生成要时间）。② 确认第 3 步分支是 `main`、目录 `/（根目录）`。③ 确认网址末尾有 `/movie-diary/`，且用户名拼写正确。④ 如果你在第 2 步把 `movie-diary` 文件夹整个拖进去了（顶层多了一层 `movie-diary`），网址要改成 `https://用户名.github.io/movie-diary/movie-diary/`。建议重传，去掉这层。

**Q：上传时 `css` / `js` / `assets` 这三个文件夹没出现，只剩一堆散文件？**
A：说明你是用"选用你的文件"逐个选的，丢了目录结构。删掉重来，用**拖拽整个 movie-diary 文件夹内的内容**的方式，让 GitHub 自己保留文件夹。

**Q：我想连代码也保密，不想公开？**
A：那需要把 GitHub 账号升级成 Pro（$4/月）才能给私人仓库开 Pages。数据本身始终是私有的，与公开与否无关。要不要升级你定。

**Q：以后我想改 app（比如加功能）怎么办？**
A：改完电脑上的文件后，回到 GitHub 这个仓库，找到对应文件点进去 → 点右上角铅笔图标「编辑」改完提交；或重新走第 2 步把新文件上传替换。等一两分钟网址就更新了。

**Q：不部署、只想在电脑上先看看？**
A：直接双击 `D:/工作空间/movie-diary/index.html` 用浏览器打开就行（部分浏览器在本地模式下不允许"离线缓存"，属正常，不影响记录功能）。

**Q：我改了代码重新上传，手机上怎么还是旧的？**
A：正常情况下不会——本项目的 service worker 用的是「网络优先」策略，线上一更新，手机下次打开自动就是新版。如果极端情况（比如手机长时间断网后缓存没刷）仍显示旧版，两种办法：① 手机上把 App 从后台划掉重开；② 重新上传时，把 `sw.js` 文件第 2 行的版本号 `movie-diary-v3` 改成 `movie-diary-v4` 再传一次，强制手机刷新离线包。

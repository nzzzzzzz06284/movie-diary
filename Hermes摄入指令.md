# 观影手记 → 知识库 自动摄入指令（Hermes 用）

本文件给 Hermes（了了的另一个 agent）使用：读取「观影手记」app 的数据，按 zlng vault 的 `AGENTS.md` 规则，把每部电影的文字内容摄入到「生活杂学 / 电影鉴赏 /」。

---

## 一、数据来源（中间文件）

观影手记 app 的数据存在浏览器 IndexedDB，外部读不到。必须有落盘的 json 文件作为中间文件：

- **自动同步模式**：Edge / Chrome 桌面版 app → 设置 →「选择同步文件夹」后，会持续更新 `<同步文件夹>/movie-diary-latest.json`
- **手动模式**：设置页「导出备份」生成的 json（命名 `观影手记备份_日期.json`）

路径确认：首次由用户在 app 里选。Hermes 侧也需知道这个路径（建议固定到 `D:\观影手记备份\` 或 vault 内的某个目录）。若用户用导出备份，则从聊天里收到的 json 读取。

json 结构（节选）：
```json
{
  "app": "movie-diary",
  "version": 2,
  "exportedAt": "2026-08-23T...",
  "records": [
    {
      "id": "...", "title": "电影名",
      "tags": ["剧情", "科幻"],
      "entries": [
        { "seq": 1, "watchDate": "2024-01-01", "rating": 5,
          "review": "观影感受原文", "comment": "短评原文",
          "quotes": ["台词原文1", "台词原文2"] },
        { "seq": 2, "watchDate": "", "dateUnknown": true, "dateNote": "2020前后",
          "rating": 0, "review": "", "comment": "", "quotes": [] }
      ],
      "watchDates": ["2024-01-01"], "watchedDate": "2024-01-01"
    }
  ],
  "screenshots": [ { "id": "...", "recordId": "...", "dataURL": "data:image/..." } ]
}
```

---

## 二、读取字段（只收文字，截图不进）

对 `records` 中每条电影，提取：

| 字段 | 含义 | 备注 |
|------|------|------|
| `title` | 片名 | 作为条目标题 |
| `entries[].watchDate` / `dateUnknown` / `dateNote` | 观影时间 | 记不清时显示备注或"记不清了" |
| `entries[].rating` | 评分 | 按次取，可列多刷评分 |
| `entries[].review` | 观影感受 | 原话保留 |
| `entries[].quotes` | 喜欢的台词 | 原话保留，逐条列出 |
| `entries[].comment` | 评论 / 短评 | 原话保留 |
| `tags` | 类型标签 | 如 剧情/科幻 |

**不摄入 `screenshots`（截图 / 最美定格）**——隐私考虑，避免人脸或剧照经微信端外泄。

---

## 三、摄入规则（遵循 AGENTS.md）

- **归属线**：生活杂学 → 电影鉴赏（非职业主线，不进 Raw/Wiki 职业侧）
- **每条电影生成** `生活杂学/电影鉴赏/<片名>.md`，字段：
  - 一句话简介（来自 `overview`，无则留空）
  - 观影时间（含「二刷 / 三刷」标注、记不清显示备注）
  - 评分
  - 观影感受（原话）
  - 喜欢的台词（原话，逐条）
  - 短评（原话）
  - 类型 `#tag`
- **更新** `生活杂学/索引.md`：加该条目 + `#影视` 标签，维护"待核实 / 已核实"清单（电影内容默认已核实，标 ✅）
- **摘要写作规范**：保留原文具体片名、原话、评分数据，不 AI 包装（不用"核心洞察"之类），直接呈现了了写了什么
- **不擅自修改** vault 其他已有页面；新增条目后按 2+1 双向链接规则补链接（生活杂学线可轻量处理）
- 不写 `Raw/`、不动职业主线（招聘/课程/简历）

---

## 四、触发方式（由 Hermes 能力决定，二选一）

1. **手动**：用户说"把观影手记更新进知识库"时执行本流程
2. **定时**（若 Hermes 支持定时任务）：每周读一次 `movie-diary-latest.json`，与「生活杂学/电影鉴赏/」已有条目比对，新增缺失的、更新变化的

---

## 五、注意事项

- 截图 **绝不** 进知识库
- 若读取 `latest.json` 失败：提示用户确认 ① 同步文件夹是否已选、② app 是否已同步（设置页「立即同步」）、③ 文件路径是否正确
- 内容不确定时标 ❓，不擅自判定
- 跨设备：手机端记的电影在手机浏览器，不会自动到电脑的同步文件夹；需用户先在手机导出、再在桌面端导入或传给 Hermes
